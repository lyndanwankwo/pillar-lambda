/*
Validate the schema coming into the API body
*/

const { AccountContext } = require("./accountContext");

//Validate the info coming in in each type of call
//Create another aux data push to keep track of historical data
